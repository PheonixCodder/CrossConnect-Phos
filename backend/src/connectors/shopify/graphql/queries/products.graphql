query FetchProducts {
  products(first: 250) {
    nodes {
      id
      title
      status
      descriptionPlainSummary

      variants(first: 100) {
        nodes {
          id
          sku
          price
        }
      }
    }
  }
}
